---
interface Props {
    fullHeight?: boolean;
}
const { fullHeight = false } = Astro.props;
---

<section class:list={["flex justify-center items-center", fullHeight && "h-[calc(90vh-4rem)]"]}>
    <form
    action="/api/auth/signin"
    method="post"
    >
        <button
        value="github"
        name="provider"
        type="submit"
        class="py-2 px-4 bg-[#1E1E1E] hover:bg-buttonHov text-white font-bold rounded-full transition duration-300 ease-in-out"
        >
            Iniciar sesión con GitHub
        </button>
    </form>
</section>
