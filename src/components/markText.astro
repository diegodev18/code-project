---
interface Props {
  color?: string;
  weight?: string;
}
const { color = "light-primary", weight = "semibold" }: Props = {};
---

<span class:list={[`text-${color}`, `font-${weight}`]}>
  <slot />
</span>
